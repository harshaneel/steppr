// Steppr Go worker SDK.
//
// Standalone module so workers can depend on the SDK without pulling in
// the orchestrator. Mirror of the Python (sdk/python) and TypeScript
// (sdk/typescript) SDKs in shape and wire protocol.
module github.com/harshaneel/steppr/sdk/go

go 1.22
